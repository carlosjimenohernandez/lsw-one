{
  "name": "@allnulled/v-descriptor",
  "version": "1.0.0",
  "description": "Inject CSS classes with meaning. For vue.js (v2).",
  "main": "v-descriptor.js",
  "scripts": {
    "build": "cp v-descriptor.js docs/v-descriptor.js",
    "up": "npm run build && bash up.sh",
    "versionate": "npm run build && bash versionate.sh",
    "test": "echo 'no test from node.js for v-descriptor'",
    "test-browser": "npm run build && npx http-server -c-1 docs -o index.html"
  },
  "keywords": ["vue2", "vue", "css"],
  "author": "allnulled",
  "license": "WTFPL"
}
